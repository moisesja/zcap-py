# Task 2026-03-26 21:53:27

## Objective

- [x] Validate whether capability parsing is still not zcap-ld compliant for root documents.

## Plan

- [x] Inspect the current root capability parsing logic and tests.
- [x] Compare the behavior against the root zcap rules in the zcap-ld spec.
- [x] Record the updated verdict and remaining gaps.

## Review

- The old finding is no longer fully accurate: a minimal spec-shaped root capability now parses successfully, and missing `@context` is rejected.
- Root-capability parsing is still not fully zcap-ld compliant:
  - The parser tolerates extra root fields such as `type` and `allowedAction`, even though the spec says a root zcap MUST NOT have any other fields.
  - The parser/model only accept a single DID string for `controller`, while the spec allows a string or an array of URI strings.
- Verification completed:
  - Minimal root probe parsed successfully.
  - Root without `@context` probe fails with `ZcapParseError(field='@context')`.
  - Root with `controller` as an array fails with `ZcapParseError(field='controller')`.
  - `uv run pytest tests/test_parser.py -q` → 61 passed.
