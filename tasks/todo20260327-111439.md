# Task 2026-03-27 11:14:39

## Objective

- [x] Re-evaluate whether the previously reported Phase 3 security and W3C zcap-ld compliance findings have been addressed.

## Plan

- [x] Inspect the current hardening changes relative to the prior review.
- [x] Re-check each previously reported issue against the current implementation.
- [x] Run focused verification and relevant tests.
- [x] Record which findings are fixed, which remain, and any new residual risks.

## Review

- Scope reviewed:
  - Branch `phase-3` at `e18e07c` (`Phase 3 hardening`)
  - Re-checked against the six findings recorded in `tasks/todo20260326-232130.md`
- Result:
  - Addressed:
    - Ancestor caveat bypass
    - Chain-to-capability linkage bypass
    - Query attenuation bypass
    - Absolute expiry bypass
    - Missing `capabilityAction` bypass
    - Embedded capability parsing and optional W3C proof dispatch
  - Still partially open:
    - Full delegated `capabilityChain` interoperability is not complete. `proof.capabilityChain` now parses, and all-embedded chains auto-resolve, but string references still raise `InvocationError` because no document loader exists yet.
- Verification:
  - `uv run pytest tests/test_verifier.py tests/test_invocation.py tests/test_target_attenuation.py tests/test_delegation.py tests/test_parser.py tests/test_proof.py -q` → `158 passed`
  - Focused re-probes:
    - `ancestor_caveat_status=BLOCKED CaveatError`
    - `chain_linkage_status=BLOCKED InvocationError`
    - `query_attenuation_status=False`
    - `expiry_status=BLOCKED CapabilityExpiredError`
    - `capability_action_status=BLOCKED InvocationError`
    - `embedded_capability_status=True`
    - `w3c_dispatch_status=WORKS`
