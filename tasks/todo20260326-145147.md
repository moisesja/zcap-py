# Task 2026-03-26 14:51:47

## Objective

- [x] Create a GitHub issue in `zcap-py` for the current `resolve_did_key()` DID URL handling behavior.

## Plan

- [x] Confirm repository context and inspect the relevant implementation/design docs.
- [x] Draft a detailed issue body with implementation guidance and acceptance criteria.
- [x] Create the GitHub issue and record the result.

## Review

- Confirmed the repo has no prior open issues, so this was filed as the first issue.
- Reproduced the inconsistency locally: `parse_did_url()` rejects a mismatched fragment while `resolve_did_key()` succeeds by stripping the fragment and resolving the bare DID.
- Created GitHub issue `#1`: `Make resolve_did_key() validate DID URLs instead of silently stripping fragments`
- Issue URL: `https://github.com/moisesja/zcap-py/issues/1`
