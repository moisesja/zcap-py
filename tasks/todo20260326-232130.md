# Task 2026-03-26 23:21:30

## Objective

- [x] Review Phase 3 for security vulnerabilities and W3C zcap-ld compliance issues.

## Plan

- [x] Identify the exact Phase 3 diff relative to `main`.
- [x] Read the relevant project design and specification notes for Phase 3 behavior.
- [x] Inspect the changed implementation and tests for security and spec-compliance gaps.
- [x] Run focused verification to confirm or disprove potential findings.
- [x] Record findings and residual risks.

## Review

- Scope reviewed:
  - Branch `phase-3` at `cd8bf6b` relative to `main` / `v0.3.0`
  - Phase 3 modules: `delegation.py`, `invocation.py`, `target_attenuation.py`, `caveats.py`, `verifier.py`
- Findings confirmed:
  - Ancestor caveats are not enforced during invocation; only the leaf capability's `caveat` list is evaluated.
  - `ZcapVerifier.verify_invocation(..., chain=...)` does not ensure the verified chain terminates at the `capability` being invoked.
  - Query-based attenuation is overly permissive; parent query restrictions can be replaced while still passing `PathPrefixAttenuator`.
  - Delegated capability expiration is never checked at invocation time; the injected `clock` is currently unused.
  - The invocation verifier accepts proofs without `capabilityAction`, even though the current W3C ZCAP-LD draft requires it for DI-proof invocations.
  - Phase 3 is not fully W3C ZCAP-LD compliant: delegated DI invocations that embed the full capability are rejected, delegation proofs do not parse/validate `capabilityChain`, and the verification path is hardwired to JCS instead of the W3C Ed25519Signature2020 algorithm.
- Verification run:
  - `uv run pytest tests/test_delegation.py tests/test_invocation.py tests/test_target_attenuation.py tests/test_caveats.py -q` → `58 passed`
  - `uv run pytest tests/test_parser.py tests/test_proof.py -q` → `87 passed`
  - Focused probes:
    - `parent_caveat_bypass=PASSED`
    - `unrelated_chain_bypass=PASSED`
    - `expired_capability_bypass=PASSED`
    - `query_attenuation_result=True`
    - `embedded_capability_invocation=REJECTED ZcapParseError`
    - `w3c_delegation_proof=REJECTED ChainVerificationError SignatureVerificationError`
