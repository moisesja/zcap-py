# Task 2026-03-26 22:09:48

## Objective

- [x] Recheck whether caveats are still parsed fail-open.

## Plan

- [x] Inspect the current caveat parsing logic and parser tests.
- [x] Run focused probes for the previously fail-open caveat cases.
- [x] Record whether the finding still applies.

## Review

- The prior fail-open caveat finding no longer applies.
- Current behavior:
  - Non-list `caveat` values raise `ZcapParseError(field="caveat")`.
  - Non-object entries inside `caveat` arrays raise `ZcapParseError(field="caveat")`.
  - Absent `caveat` still defaults to `[]`, which is appropriate.
- Verification completed:
  - `uv run pytest tests/test_parser.py -q` → 70 passed
  - Direct probe with `caveat: ["not-an-object"]` → `ZcapParseError`
  - Direct probe with `caveat: "not-a-list"` → `ZcapParseError`
