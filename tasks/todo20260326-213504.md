# Task 2026-03-26 21:35:04

## Objective

- [x] Recheck whether the prior "Invocation parsing is fail-open" finding still applies.

## Plan

- [x] Inspect the current invocation parsing code and parser tests.
- [x] Run focused verification against the previous failure mode.
- [x] Record whether the finding still stands.

## Review

- The previous finding no longer applies as written.
- `parse_invocation()` now calls `_parse_proof(..., "capabilityInvocation")` and rejects invocation proofs that:
  - omit `proof.proofPurpose`
  - use the wrong `proof.proofPurpose`
  - omit `proof.capability`
- Verification completed:
  - `uv run pytest tests/test_parser.py -q` → 50 passed
  - Direct probe of an invocation proof missing `proof.proofPurpose` now raises `ZcapParseError(field="proof.proofPurpose")`
- Nearby residual risk noted for future review: the parser requires both top-level `capability` and proof-level `capability`, but this recheck did not confirm whether it enforces equality between them.
