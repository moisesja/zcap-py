# Task 2026-03-26 16:06:49

## Objective

- [x] Review Phase 2 (`JCS, Proof Verification & Parser`) for security vulnerabilities and W3C zcap-ld compliance issues.

## Plan

- [x] Inspect the Phase 2 implementation, tests, and diff against `main`.
- [x] Validate the implementation behavior with focused local verification.
- [x] Compare the observed behavior against the W3C zcap-ld specification and record findings.

## Review

- Reviewed commit `923eeec` (`feature/phase2-jcs-proof-parser`) against the local Phase 2 design, the W3C zcap-ld draft, and the W3C EdDSA / Ed25519Signature2020 cryptosuite specification.
- Local verification completed:
  - `uv run pytest` → 130 passed
  - `uv run pytest tests/test_jcs.py tests/test_proof.py tests/test_parser.py` → 72 passed
  - `uv run ruff check .` → passed
  - `uv run mypy src tests` → failed in `tests/test_proof.py` due two incorrect `type: ignore` usages around `dict(doc["proof"])`
- Confirmed findings:
  - `verify_document_proof()` performs JCS-based verification with a caller-supplied key instead of the Ed25519Signature2020 / Data Integrity verification flow, and it accepted a document signed by key B while claiming key A in `proof.verificationMethod`.
  - `ZcapParser` rejects spec-shaped root zcaps and accepts non-spec capability documents with missing `@context`.
  - `ZcapParser` accepts invocations whose proof omits required `proofPurpose` / `capability` fields, defaulting `proofPurpose` to `capabilityDelegation`.
  - Malformed `caveat` entries are silently dropped instead of causing parse failure.
